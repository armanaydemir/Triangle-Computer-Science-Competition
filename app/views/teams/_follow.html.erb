<%= form_for(current_team.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @f_team.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>